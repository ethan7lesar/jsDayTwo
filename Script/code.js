let subject = [ 'Python', 'C++',
    'VB.Net', 'Javascript', 'Assembly'];
let size = subject.length;
// console.log(subject.pop());

console.log(subject[size - 1]);

console.log(subject[2]);

// document.write(subject[size-1]);

